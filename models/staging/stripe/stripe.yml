version: 2

models:
  - name: stripe_payments
    columns:
      - name: payment_id
        columns:
          - name: id
            tests:
              - not_null
              - unique

          - name: order_id
            description: Foreign key to jaffle_shop orders

          - name: amount
            description: Amount in US cents